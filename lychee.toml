# Safety net: relative paths like ../../issues resolve to non-existent file://
# paths when lychee runs. The README now uses absolute GitHub URLs instead,
# but this exclude prevents false positives if any relative issue links remain.
exclude = ["file:///.*issues$"]
